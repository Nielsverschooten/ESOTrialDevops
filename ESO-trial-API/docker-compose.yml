version: '3.4'

services:
  db:
    image: "mcr.microsoft.com/mssql/server:2017-latest"
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=NiDevveOps
  eso-trial-api:
    image: ${DOCKER_REGISTRY-}esotrialapi
    build:
      context: .
      dockerfile: ESO-trial-API/Dockerfile
    depends_on:
      - db